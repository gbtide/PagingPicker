<resources>

    <!-- Widget styles -->
    <style name="Widget.PagingPicker" parent="@android:style/Widget.Material" />

    <style name="Widget.PagingPicker.AppBar" parent="@style/Widget.Design.AppBarLayout">
        <item name="android:background">?attr/colorSurface</item>
        <item name="android:elevation">8dp</item>
    </style>

    <!-- 리스트 빈 화면 나올 때 -->
    <style name="Widget.PagingPicker.EmptyResults">
        <item name="android:drawablePadding">@dimen/margin_normal</item>
        <item name="android:drawableTop">@drawable/ic_logo_assistant</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
    </style>

</resources>